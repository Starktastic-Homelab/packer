name: Lint and Validate Packer Template

on:
  pull_request:
    branches:
      - main

jobs:
  packer-lint-check:
    name: Packer Lint & Validate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install Packer
        uses: hashicorp/setup-packer@v2
        with:
          version: latest

      - name: Format and validate Packer files
        run: |
          packer fmt -recursive .

          if [ -n "$(git status --porcelain)" ]; then
            git add .
            git commit -m "style: packer fmt"
            git remote set-url origin https://x-access-token:${{ secrets.PAT_PUSH }}@github.com/${{ github.repository }}
            git push origin HEAD:"${{ github.head_ref || github.ref_name }}"
          else
            echo "No formatting changes to commit."
          fi

      - name: Validate Packer template
        run: packer validate .
