name: Format

on:
  workflow_dispatch:
  pull_request:
    branches: [main]

jobs:
  format:
    name: Format Packer, YAML, JSON and shell files
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          ref: ${{ github.head_ref }}
          token: ${{ secrets.ORG_DISPATCH_TOKEN }}

      - name: Install Packer
        uses: hashicorp/setup-packer@v3
        with:
          version: latest

      - name: Run Packer fmt command
        run: packer fmt -recursive .

      - name: Commit & push formatting changes
        if: steps.fmt.outputs.changed_files != ''
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

          git add .
          git commit -m "[skip ci] style: packer fmt"
          git push

      - name: Prettier
        uses: creyD/prettier_action@v4.6
        with:
          prettier_options: --write **/*.{yaml,yml,json}
          no_commit: true

      - name: Setup shfmt
        uses: mfinelli/setup-shfmt@v4.0.1

      - name: Run shfmt
        run: shfmt -i 2 -ci -s -w **/*.sh || true

      - name: Commit changes
        uses: stefanzweifel/git-auto-commit-action@v7.1.0
        with:
          commit_message: "style: apply automatic formatting"
